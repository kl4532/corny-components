import { CButtonDirective } from './c-button.directive';

describe('CButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CButtonDirective();
    expect(directive).toBeTruthy();
  });
});
